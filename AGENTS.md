# AGENTS.md

このリポジトリでコードを書く全ての自動エージェント（および人間）向けの規約です。
特定のAIサービスに依存しません。作業を始める前に必ず読んでください。

タスク固有の仕様は `docs/` にあります。**この文書は全タスクに共通する制約**です。

| 文書 | 内容 |
|---|---|
| `AGENTS.md`（これ） | 全作業に共通する規約・検証手順・既知の制約 |
| `docs/ARCHITECTURE.md` | 長期設計。能力モデル、測定方針、実装フェーズ |
| `docs/TASK-P0.md` | 現在の実装タスクの詳細仕様 |

---

## 1. このプロジェクトは何か

スマートフォン向けの指弾きギター練習アプリ。GitHub Pages で静的配信されます。
現在は「教材を表示して練習を記録する」段階で、**演奏能力を測定・処方する訓練システム**へ拡張中です（`docs/ARCHITECTURE.md`）。

```
index.html   基礎     10分レッスン。TAB譜とメトロノーム        guitar.js  / guitar.css
phrase.html  フレーズ  五線譜 + TAB + Web Audio再生・伴奏      phrase.js  / phrase.css
rhythm.html  リズム   口・右手・左手・足の独立練習            rhythm.js / rhythm.css / rhythm/
sw.js        オフライン用 Service Worker
data/        教材JSON（唯一の教材ソース）
scripts/     データ検証。npm test から実行される
tests/       Playwright によるブラウザテスト
```

---

## 2. 変更してはいけない前提

以下はプロジェクトの土台です。破る変更は受け付けられません。

### ビルド工程を持たない

**バンドラ・TypeScript・フレームワーク・実行時npm依存を導入しないでください。**
リポジトリのファイルがそのまま配信されます。`package.json` の依存は開発時のテストランナーのみです。

「この方が現代的だから」を理由にした構成変更は却下されます。オフライン動作とデプロイの単純さが、
このアプリにとっては現代性より価値があります。

### 教材はデータ、コードではない

レッスンとフレーズは `data/**` のJSONです。**教材を追加するのにコードを書く必要はありません。**
逆に、教材固有の分岐をコードへ書き込まないでください。

### 時間の基準は常にオーディオクロック

メトロノーム、再生、将来の測定は全て `AudioContext.currentTime` を基準に先読みスケジュールします。
**`setInterval` / `setTimeout` でイベントを発音しないでください。**
タイマーはスケジューラのキューを補充する用途にのみ使います。

訓練の基準になる時計自身が揺れてはいけない、というのがこのアプリの根本要件です。

### `core/` はグローバル環境に依存しない

`core/` に置くモジュールは、**`document` / `window` / `AudioContext` をグローバルから掴んではいけません。**
必要なものは引数で受け取ります。これはテスト可能性と再利用性のための境界です。

**「`core/` はDOMを触らない」ではない**ことに注意してください。境界はモジュールごとに違います。

| モジュール | 境界 |
|---|---|
| `music.js` | 完全にプラットフォーム非依存の純ロジック。Nodeから直接テストできる |
| `clock.js` | `AudioContext` を引数で受け取る。グローバル参照なし |
| `notation.js` | **DOMアダプタ。DOMは作る。** ただし画面固有のUIや状態は知らず、`host.ownerDocument` 経由でのみ要素を生成する |

`notation.js` がDOMを生成するのは設計違反ではありません。要件は
**画面から独立していること**であって、DOMに触れないことではありません。

### 譜面のDOM契約

ブラウザテストが依存しています。要素名・クラス名・属性を勝手に変えないでください。

```
.staff-system  .staff-line  .bar-line  .ledger
.note-symbol   .note-head   .note-head.open   .note-stem   .note-flag
.note-name-text  .finger-text  .accidental  .key-signature
.clef  .time-sig  .measure-no  .chord-symbol

data-measure   data-staff-line   data-note-index   data-note-name
role="button"  tabindex="0"
```

1小節 = 1つの `<svg class="staff-system">`。調号は全システムに描画されます。

---

## 3. 検証

**作業完了を報告する前に、以下を実際に実行して出力を確認してください。**

```bash
npm install                      # 開発依存（Playwright）
npx playwright install chromium

node --check guitar.js && node --check phrase.js && node --check rhythm.js && node --check sw.js
find core rhythm -type f -name '*.js' -exec node --check {} \;
npm test                         # 単体テスト + データ／シェル検証3種
npm run test:e2e                 # ブラウザ回帰テスト
```

`npm test` は `tests/unit/*.test.mjs` の単体テスト後、3つの検証を順に走らせます。

| 検証 | 内容 |
|---|---|
| `validate-lessons.mjs` | レッスンJSONと索引の整合、分数合計、TAB弦順、MusicXMLの存在 |
| `validate-phrases.mjs` | 音名とTABの一致、調・拍・音価、右手運指の対応 |
| `validate-shell.mjs` | **各ページが読む全アセットが `sw.js` の `APP_SHELL` にあるか** |

手元で見る場合は `npm run serve` → `http://localhost:8000`。
`file://` では教材JSONもService Workerも動きません。

### テストを緩めない

落ちたテストを `skip` したり、アサーションを弱めたり、閾値を動かして通すことは禁止です。
**テストが落ちたら実装を直してください。** 仕様側が誤っていると考える場合は、勝手に直さず報告してください。

---

## 4. 新しいファイルを追加するとき

**ローカルアセット（JS / CSS / 画像 / フォント）を追加したら、必ず `sw.js` に登録してください。**

1. `APP_SHELL` にパスを追加
2. `CACHE_NAME` のバージョンを1つ上げる

忘れるとオフライン時に無言で壊れます。**実際に起きたバグです。**
`npm test` の `validate-shell.mjs` が検出するので、通ることを確認してください。

---

## 5. 既知の制約

「バグかと思ったら仕様だった」を避けるための一覧です。

| 事項 | 現状 |
|---|---|
| **記譜オクターブ** | ギターは記譜より1オクターブ低く鳴ります。譜面はト音記号の `8vb`、再生は実音で扱います |
| **フレーズの拍子** | データ検証と伴奏パターンは現在4/4のみ。単音再生は音符の拍位置を基準にし、32分音符・付点・連符に対応 |
| **リズムの拍子** | パターン定義から導出。4/4・3/4・5/4・6/8・7/8・12/8を表現可能 |
| **音価** | 全音符〜32分音符、付点、タイ、時間変更と表示を分けた連符に対応。休符は未対応 |
| **調** | `core/music.js` の `SUPPORTED_KEYS` にある調のみ。バリデータが他を拒否します |
| **声部** | 単旋律のみ。複声部は未対応 |
| **譜面ライブラリ** | VexFlow 5 / Bravura をリポジトリに同梱。自作のDOMオーバーレイでタップ、再生追従、既存のDOM契約を維持 |

---

## 6. 書き方の約束

- **UIの文字列と検証エラーメッセージは日本語。** 利用者は日本語話者です
- **コード内のコメントは、そのファイルの既存コメントに合わせてください**（現状は英語）
- コメントには「何をしているか」ではなく「**なぜそうなっているか**」を書く
- 既存ファイルのスタイル（インデント、命名、密度）に合わせる。整形し直さない
- キーボード操作可能であること、フォーカスが見えること
- モバイル基準（390px幅）。ページ本体が横スクロールしないこと

---

## 7. 作業の進め方

1. `main` から作業ブランチを切る
2. **論理的な単位でコミットを分ける。** 各コミット時点でテストが通る状態を保つ
3. コミットメッセージは、何を変えたかではなく**なぜ変えたか**を書く
4. 指示されていない「ついでの改善」をしない。差分が読めなくなります

### 完了報告に含めるもの

- コミットのハッシュと一覧
- `npm test` と `npm run test:e2e` の**実行結果そのまま**
- 仕様に曖昧さがあり自己判断で埋めた箇所と、その理由
- 移設・機械的作業ではなく**判断を要した箇所**の一覧

判断した内容を黙って埋めないでください。レビューで最も時間を食うのはそこです。
